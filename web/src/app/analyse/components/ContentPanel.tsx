"use client";

import React from "react";

export type ContentPanelProps = {
  currentIndex: number | null;
};

export default function ContentPanel({ currentIndex }: ContentPanelProps) {
  return <div>ContentPanel</div>;
}
