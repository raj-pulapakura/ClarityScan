import React from "react";

export default function Footer() {
  return (
    <div className="w-full fixed bottom-0 left-0 p-3 bg-gray-400 bg-opacity-60">
      Note: If the app stops working, it's because I have run out of credits on
      Modal.
    </div>
  );
}
